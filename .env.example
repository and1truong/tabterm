# Bun server port (HTTP + app WS + GoTTY proxy)
PORT=3000

# Base port for dynamically-allocated GoTTY processes (v0.2+)
GOTTY_BASE_PORT=4001

# Anthropic API key for the Assistant panel (v0.4+). Never sent to the client.
ANTHROPIC_API_KEY=
